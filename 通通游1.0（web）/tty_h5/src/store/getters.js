export const searchKeywords = state => state.searchKeywords

export const searchHistory = state => state.searchHistory

export const productInfo = state => state.productInfo

export const tripInfo = state => state.tripInfo

export const selectedVisitors = state => state.selectedVisitors

export const editVisitor = state => state.editVisitor

export const serverStatus = state => state.serverStatus
